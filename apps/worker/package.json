{
  "name": "@void-space/worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "BullMQ background processors for VOID·SPACE — AI enrichment, IPFS pinning, Blender optimisation, chain licensing, XR publishing and notifications (SRS §3.10).",
  "main": "./src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@void-space/db": "workspace:*",
    "@void-space/types": "workspace:*",
    "bullmq": "^5.34.10",
    "ioredis": "^5.4.2",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0",
    "viem": "^2.56.8",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
