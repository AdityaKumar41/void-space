{
  "name": "@void-space/types",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Shared TypeScript contracts (DTOs, Zod schemas, RBAC matrix, queue contracts, generated contract ABI) used by web + api + worker. Single source of truth to prevent contract drift (SRS §3.4 / NFR-MAINT.3).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contracts": "./src/contracts/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "turbo": {
    "tasks": {
      "build": {
        "outputs": []
      }
    }
  },

  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
